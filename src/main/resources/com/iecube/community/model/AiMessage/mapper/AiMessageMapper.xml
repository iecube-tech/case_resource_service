<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace 属性：用于指定当前的映射文件和哪个接口进行映射， 需要指定接口的文件路径， 需要标注包的完整路径接口-->
<mapper namespace="com.iecube.community.model.aiMessage.mapper.AiMessageMapper">

    <resultMap id="AiMessage" type="com.iecube.community.model.aiMessage.entity.AiMessage">
        <result column="chat_id" property="chatId"></result>
        <result column="create_time" property="createTime"></result>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_message (`id`, `chat_id`, `role`, `content`, `create_time`)
        VALUES (#{id}, #{chatId}, #{role}, #{content}, #{createTime})
    </insert>
</mapper>

